📘 Jenkins Pipeline for Python Project (python-pipeline)
🚀 Objective

Create a Jenkins pipeline named python-pipeline that integrates with a Git repository and automates:

Code checkout
Dependency installation
Test execution (controlled via environment variable)
Build / application execution
